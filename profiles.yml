jaffle_shop: # this needs to match the profile in your dbt_project.yml file
  target: dev
  outputs:
    dev:
      type: duckdb
      path: 'C:\GitRepos\dbt-test.duckdb'
      extensions:
        - httpfs
        - parquet